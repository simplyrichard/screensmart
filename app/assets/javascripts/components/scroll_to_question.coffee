# $(document).ready ->
#   $('.option').on 'click', (e) ->
#     e.preventDefault()
#     offset = $(this).next().find('.empty_question').offset().top
#     $('html, body').stop().animate { scrollTop: offset }, 400
#     return
#   return
